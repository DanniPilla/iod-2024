// Create an array that has 5 elements
let catBreeds = ["Bengal", "Ragdoll", "Savannah Cat", "Maine Coon", "Siamese cat"];
console.log(catBreeds[0]); 
console.log(catBreeds[1]); 
console.log(catBreeds[2]);
console.log(catBreeds[3]);
console.log(catBreeds[4]);

// Replace the value of the element at position 1 and 4
catBreeds [1] = "Oselot"
catBreeds [4] = "British Shorthair"
console.log(catBreeds);

// Add a new element to the beginning of the array
catBreeds.unshift("Persian")
console.log(catBreeds);

// Remove the element at the end of the array
catBreeds.pop("British Shorthair");

// Print the array to the console
console.log(catBreeds);